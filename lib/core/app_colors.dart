import 'dart:ui';

class AppColors {
  static const Color primary = Color(0xFF13A4EC); // solid color
  static final Color primary20 = const Color(
    0xFF13A4EC,
  ).withOpacity(0.2); // 20% opacity
  static final Color primary30 = const Color(
    0xFF13A4EC,
  ).withOpacity(0.3); // 30% opacity for dark mode
}
